# ĐẶC TẢ CHI TIẾT MODULE P2 (PERSON) - QUẢN LÝ & TÍNH LƯƠNG NĂNG LỰC CÁ NHÂN
**Tác giả:** Ngô Quang Anh  
**Chuyên ngành:** Kinh tế số (Digital Economics)  
**Kiến trúc mã nguồn:** Phân tách cấu phần dịch vụ (Decoupled Service Sub-modules)

---

## 1. THIẾT KẾ TOÀN CỤC & ĐIỀU KIỆN RÀNG BUỘC CHẶN TRẦN (P2 CAPPING)

Mô hình lương P2 tập trung đo lường giá trị nội tại của người lao động. Nhằm kiểm soát dòng tiền và bảo vệ quỹ lương vận hành của doanh nghiệp, hệ thống áp dụng logic chặn trần nghiêm ngặt thông qua hàm toán học tại tầng xử lý tổng hợp (Payroll Aggregator Engine).

* **Công thức tổng quát:**
  $$\text{P2}_{Final} = \min\left(\text{P}_{21} + \text{P}_{22} + \text{P}_{23},\ \text{P2}_{Cap}\right)$$
* **Tham số cấu hình hệ thống:** Đối với vị trí *Backend Developer (Golang)*, hạn mức `P2_Cap` gán cố định trong Database là **5,000,000 VND**. Nếu tổng các mảnh thành phần vượt quá ngưỡng này, hệ thống sẽ tự động tịnh tiến về mức trần quy định.

---

## 2. BĂM CHI TIẾT CÁC CẤU PHẦN THÀNH PHẦN (SUB-MODULES)

```
                       ┌──► Mảnh 1: Phụ cấp Khung năng lực (P21) -> Thuật toán R_comp
                       │
[Hệ thống Core P2] ────┼──► Mảnh 2: Quản lý Chứng chỉ (P22) -> Tự động hóa quét thời hạn
                       │
                       └──► Mảnh 3: Thâm niên tự động (P23) -> Real-time Duration
```

### 🧩 Mảnh 1: Đánh giá Khung năng lực chuyên môn ($P_{21}$ - Competency Level)
Hệ thống không tính tiền level dựa trên chức danh cảm tính, mà số hóa thành một **Hệ thống đánh giá khoảng trống năng lực (Competency Gap Analysis)**.

#### Bước 1.1: Định nghĩa Ma trận năng lực yêu cầu (Target Competency Matrix)
Mỗi vị trí công việc tại một cấp bậc (Level) nhất định sẽ yêu cầu điểm số chuẩn từ 1 đến 5 trên các kỹ năng cốt lõi. Ví dụ cấu hình cho vị trí *Backend Dev (Bậc Mid)*:
1. **Kiến thức Golang (Go Core):** Yêu cầu Level 3
2. **Thiết kế Hệ thống (System Design):** Yêu cầu Level 3
3. **Quản trị Cơ sở dữ liệu (Database/ORM):** Yêu cầu Level 2
$$\text{Tổng điểm năng lực tiêu chuẩn (Target Score)} = 3 + 3 + 2 = 8\text{ điểm}$$

#### Bước 1.2: Thu thập điểm thực tế qua Kỳ sát hạch (Actual Score)
Hội đồng nhân sự định kỳ cập nhật điểm đánh giá thực tế của nhân sự dựa trên kết quả làm việc thực tiễn. Giả sử nhân viên Nguyễn Văn A được chấm:
* Kiến thức Golang: Đạt Level 4
* Thiết kế Hệ thống: Đạt Level 2
* Quản trị Cơ sở dữ liệu: Đạt Level 3
$$\text{Tổng điểm năng lực thực tế (Actual Score)} = 4 + 2 + 3 = 9\text{ điểm}$$

#### Bước 1.3: Thuật toán tính Tỷ lệ đáp ứng ($R_{comp}$) và Trả lương
Hệ thống tự động thực hiện phép chia để tìm tỷ lệ đáp ứng năng lực:
$$R_{comp} = \frac{\text{Actual Score}}{\text{Target Score}} = \frac{9}{8} = 1.125\ (112.5\%)$$

**Logic phân nhánh xử lý lương $P_{21}$ của Backend:**
* Nếu $R_{comp} < 0.8$ (Dưới 80%): Hạ phụ cấp xuống bậc Junior $\rightarrow P_{21} = 0$ VND.
* Nếu $0.8 \le R_{comp} \le 1.2$ (Đạt chuẩn bậc Mid): Hưởng 100% phụ cấp định mức $\rightarrow P_{21} = 2,500,000$ VND.
* Nếu $R_{comp} > 1.2$ (Vượt chuẩn xuất sắc): Thưởng thêm 20% phụ cấp bậc Mid $
ightarrow P_{21} = 2,500,000 \times 1.2 = 3,000,000$ VND.

---

### 🧩 Mảnh 2: Quản lý và Tự động hóa Chứng chỉ ($P_{22}$ - Certificates & Qualifications)
Mảnh này chịu trách nhiệm tự động hóa việc cộng tiền dựa trên các bằng cấp/chứng chỉ công nghệ, ngoại ngữ của nhân sự, đồng thời tự động kiểm soát **Thời hạn hiệu lực**.

#### Quy trình xử lý tự động của hệ thống (Cron Job Engine):
Hệ thống thiết lập một Background Worker chạy ngầm vào lúc 00:00 hàng ngày, thực hiện quét toán học trên toàn bộ bảng dữ liệu chứng chỉ nhân viên:
$$\text{Trạng thái Hiệu lực} = (\text{Ngày hiện tại} \le \text{Ngày cấp} + \text{Thời hạn hiệu lực})$$

* **Nếu Hợp lệ (`is_active = true`):** Khoản tiền cấu hình của chứng chỉ đó tiếp tục được nạp vào biến tính toán $P_{22}$.
* **Nếu Hết hạn (`is_active = false`):** Hệ thống tự động đẩy thông báo nhắc nhở nhân sự qua giao diện cá nhân, đồng thời **tự động loại bỏ** khoản tiền phụ cấp đó ra khỏi luồng tính toán lương của tháng tiếp theo.

*Ví dụ thực tế Nguyễn Văn A (Mảnh 2):*
* Chứng chỉ AWS Cloud Solutions Architect (Còn hạn): $+ 2,000,000$ VND
* Chứng chỉ Tiếng Anh TOEIC 800 (Còn hạn): $+ 1,500,000$ VND
$$\rightarrow P_{22} = 2,000,000 + 1,500,000 = 3,500,000\text{ VND}$$

---

### 🧩 Mảnh 3: Thuật toán tính Thâm niên tự động ($P_{23}$ - Seniority Allowance)
Mảnh này loại bỏ hoàn toàn việc HR phải đếm ngày tháng thủ công bằng cách sử dụng hàm xử lý thời gian thực (Real-time Duration Calculator).

#### Công thức toán học tính số tháng làm việc ($M_{senior}$):
$$M_{senior} = (\text{Year}_{current} - \text{Year}_{hire}) \times 12 + (\text{Month}_{current} - \text{Month}_{hire})$$

#### Logic phân cấp bậc thang lương phụ cấp thâm niên:
* Nếu $M_{senior} < 12$ tháng: $P_{23} = 0$ VND.
* Nếu $12 \le M_{senior} < 36$ tháng: $P_{23} = 200,000 \times \lfloor \frac{M_{senior}}{12} \rfloor$ VND.
* Nếu $M_{senior} \ge 36$ tháng: $P_{23} = 500,000 \times \lfloor \frac{M_{senior}}{12} \rfloor$ VND.

*Ví dụ thực tế Nguyễn Văn A (Làm việc được 24 tháng $\rightarrow M_{senior} = 24$):*
$$P_{23} = 200,000 \times \lfloor \frac{24}{12} \rfloor = 200,000 \times 2 = 400,000\text{ VND}$$

---

## 3. KHÁI QUÁT CHUỖI LOGIC TRÊN BACKEND (GOLANG ARCHITECTURE)

Đoạn mã cấu trúc mô phỏng luồng băm nhỏ và tính toán chi tiết cấu phần P2:

```go
package payroll

import "math"

type CompetencyProfile struct {
	ActualScore   float64
	TargetScore   float64
	BaseAllowance float64 // Mức phụ cấp gốc của bậc (ví dụ: 2,500,000 VND)
}

type EmployeeCertificate struct {
	Amount   float64
	IsActive bool // Được quyết định bởi Cron job kiểm tra thời hạn
}

// CalculateP2Engine xử lý băm nhỏ và tổng hợp cấu phần lương P2
func CalculateP2Engine(comp CompetencyProfile, certs []EmployeeCertificate, monthsSenior int, p2Cap float64) float64 {
	// 1. Xử lý Mảnh P21 (Khung năng lực chuyên môn)
	p21 := 0.0
	rComp := comp.ActualScore / comp.TargetScore
	if rComp >= 0.8 && rComp <= 1.2 {
		p21 = comp.BaseAllowance
	} else if rComp > 1.2 {
		p21 = comp.BaseAllowance * 1.2
	}

	// 2. Xử lý Mảnh P22 (Tự động hóa tích hợp chứng chỉ còn hạn)
	p22 := 0.0
	for _, cert := range certs {
		if cert.IsActive {
			p22 += cert.Amount
		}
	}

	// 3. Xử lý Mảnh P23 (Tính toán thâm niên theo thời gian thực)
	p23 := 0.0
	if monthsSenior >= 12 {
		years := monthsSenior / 12
		p23 = float64(years) * 200000.0
	}

	// 4. Tổng hợp dữ liệu đầu ra và thực hiện Chặn Trần (Capping Logic)
	totalP2Real := p21 + p22 + p23
	return math.Min(totalP2Real, p2Cap)
}
```

---
## 4. MÔ HÌNH DỮ LIỆU ĐA QUAN HỆ SỐ HÓA CẤU PHẦN P2 (POSTGRESQL)

```sql
-- Cấu hình danh mục tiêu chí đánh giá năng lực của Mảnh 1
CREATE TABLE competency_skills (
    id SERIAL PRIMARY KEY,
    skill_name VARCHAR(100) NOT NULL -- Golang Core, System Design, Database...
);

-- Ma trận điểm chuẩn yêu cầu ứng với từng vị trí công việc
CREATE TABLE position_target_competencies (
    position_id INT REFERENCES positions(id),
    skill_id INT REFERENCES competency_skills(id),
    target_score INT CHECK (target_score BETWEEN 1 AND 5),
    PRIMARY KEY (position_id, skill_id)
);

-- Điểm số thực tế của nhân viên sau mỗi kỳ khảo sát sát hạch
CREATE TABLE employee_actual_competencies (
    employee_id INT NOT NULL,
    skill_id INT REFERENCES competency_skills(id),
    actual_score INT CHECK (actual_score BETWEEN 1 AND 5),
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (employee_id, skill_id)
);

-- Danh mục chứng chỉ công nghệ và ngoại ngữ của Mảnh 2
CREATE TABLE certificate_catalogs (
    id SERIAL PRIMARY KEY,
    cert_name VARCHAR(150) NOT NULL,
    allowance_amount DECIMAL(15,2) NOT NULL,
    valid_duration_months INT NOT NULL -- Thời hạn hiệu lực (ví dụ: 24 tháng hoặc 36 tháng)
);

-- Bảng lưu và quản lý chứng chỉ thực tế của nhân sự
CREATE TABLE employee_certificates (
    employee_id INT NOT NULL,
    certificate_id INT REFERENCES certificate_catalogs(id),
    issued_date DATE NOT NULL,
    is_active BOOLEAN DEFAULT TRUE, -- Hệ thống tự động cập nhật qua Cron Job
    approved_by INT,
    PRIMARY KEY (employee_id, certificate_id)
);